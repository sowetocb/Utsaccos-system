apiVersion: v1
kind: Service
metadata:
  name: saccos-frontend
  namespace: saccos
  labels:
    app: saccos-frontend
spec:
  type: ClusterIP
  selector:
    app: saccos-frontend
  ports:
  - name: http
    port: 80
    targetPort: http
    protocol: TCP
  sessionAffinity: None
